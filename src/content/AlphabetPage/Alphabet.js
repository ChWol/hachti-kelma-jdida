import React from 'react';

const Alphabet = () => {
    return (
        <div style={{paddingTop: '50px'}}>
            Alphabet, delete index.js files btw, look at IBM project for optimized search and
            action buttons, just compare like everything
            Copy it all over to yours
            Eingabe von Schriftzeichen wird umgewandelt in lateinisches Alphabet
        </div>
    );
};

export default Alphabet;